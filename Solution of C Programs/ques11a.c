#include<stdio.h>
#include<string.h>
int main(){
    char str[200];
    int c = 0, w = 0, l = 0;
    printf("Enter the main string(S) :\n");
    fgets(S, sizeof(S), stdin);
    while(str[i]!='\0'){
        if(str[i]!='\n'){
            c++;
        }
        if(str[i]=='\n'){
            l++;
        }
        if()
    }
    return 0;
}